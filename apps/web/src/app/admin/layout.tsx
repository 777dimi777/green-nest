import Link from "next/link";
import { BarChart3, Boxes, Leaf, LayoutDashboard, UsersRound } from "lucide-react";
const items = [{ icon: LayoutDashboard, label: "Pregled" }, { icon: Boxes, label: "Proizvodi" }, { icon: UsersRound, label: "Korisnici" }, { icon: BarChart3, label: "Analitika" }];
export default function AdminLayout({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-muted/35 md:grid md:grid-cols-[240px_1fr]"><aside className="border-b bg-card p-5 md:min-h-screen md:border-b-0 md:border-r"><Link href="/" className="flex items-center gap-2 font-serif text-xl font-bold"><Leaf className="size-5 text-primary" />Green Nest</Link><p className="mt-2 text-xs uppercase tracking-wider text-muted-foreground">Administracija</p><nav aria-label="Admin navigacija" className="mt-8 flex gap-2 overflow-x-auto md:flex-col">{items.map(({ icon: Icon, label }, index) => <span key={label} className={`flex min-w-max items-center gap-3 rounded-lg px-3 py-2 text-sm ${index === 0 ? "bg-primary text-primary-foreground" : "text-muted-foreground"}`}><Icon className="size-4" />{label}</span>)}</nav></aside><main className="p-5 sm:p-8 lg:p-10">{children}</main></div>; }
