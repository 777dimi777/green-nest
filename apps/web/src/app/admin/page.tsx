import { Banknote, Boxes, ShoppingCart, UsersRound } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
const stats = [{ icon: Banknote, title: "Prihod", value: "—" }, { icon: ShoppingCart, title: "Porudžbine", value: "—" }, { icon: UsersRound, title: "Korisnici", value: "—" }, { icon: Boxes, title: "Proizvodi", value: "—" }];
export const metadata = { title: "Admin kontrolna tabla" };
export default function AdminPage() { return <><p className="text-sm font-medium text-primary">Pregled poslovanja</p><h1 className="mt-2 font-serif text-4xl font-semibold">Admin kontrolna tabla</h1><p className="mt-3 max-w-2xl text-muted-foreground">Struktura je spremna za budući AdminGuard i povezivanje analitike. Podaci se u ovom segmentu namerno ne učitavaju.</p><div className="mt-9 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ icon: Icon, title, value }) => <Card key={title}><CardHeader className="flex-row items-center justify-between"><CardTitle className="text-sm text-muted-foreground">{title}</CardTitle><Icon className="size-5 text-primary" /></CardHeader><CardContent><p className="text-3xl font-semibold">{value}</p><p className="mt-2 text-xs text-muted-foreground">Biće dostupno nakon API integracije</p></CardContent></Card>)}</div></>; }
