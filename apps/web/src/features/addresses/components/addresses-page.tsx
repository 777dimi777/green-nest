"use client";import{useState}from"react";import{MapPin,Trash2}from"lucide-react";import{Button}from"@/components/ui/button";import{ErrorState}from"@/components/common/error-state";import{Skeleton}from"@/components/ui/skeleton";import{getApiErrorMessage}from"@/lib/api/api-error";import type{Address}from"@/types/address";import{useAddresses,useRemoveAddress}from"../hooks/use-addresses";import{AddressForm}from"./address-form";
export function AddressesPage(){const q=useAddresses(),remove=useRemoveAddress(),[editing,setEditing]=useState<Address|null>(null),[adding,setAdding]=useState(false);if(q.isPending)return <Skeleton className="h-80"/>;if(q.isError)return <ErrorState title="Adrese nisu dostupne" description={getApiErrorMessage(q.error)} onRetry={()=>void q.refetch()}/>;return <><div className="mb-8 flex items-end justify-between"><div><h1 className="font-serif text-5xl font-semibold">Adrese</h1><p className="mt-2 text-muted-foreground">Adrese za dostavu porudžbina.</p></div><Button onClick={()=>setAdding(v=>!v)}>{adding?"Zatvori":"Nova adresa"}</Button></div>{adding&&<div className="mb-8 rounded-xl border p-5"><AddressForm onDone={()=>setAdding(false)}/></div>}<div className="grid gap-4 md:grid-cols-2">{q.data.map(a=><article key={a.id} className="rounded-xl border p-5"><div className="flex justify-between gap-4"><MapPin/><div className="flex-1"><strong>{a.firstName} {a.lastName}</strong>{a.isDefault&&<span className="ml-2 rounded-full bg-secondary px-2 py-1 text-xs">Podrazumevana</span>}<p className="mt-2 text-sm text-muted-foreground">{a.street} {a.streetNumber}{a.apartment?`, ${a.apartment}`:""}<br/>{a.postalCode} {a.city}, {a.country}<br/>{a.phone}</p></div></div><div className="mt-4 flex gap-2"><Button variant="outline" size="sm" onClick={()=>setEditing(editing?.id===a.id?null:a)}>Izmeni</Button><Button variant="ghost" size="sm" disabled={remove.isPending} onClick={()=>{if(window.confirm("Obrisati adresu?"))remove.mutate(a.id)}}><Trash2/>Obriši</Button></div>{editing?.id===a.id&&<div className="mt-5 border-t pt-5"><AddressForm address={a} onDone={()=>setEditing(null)}/></div>}</article>)}</div></>}
