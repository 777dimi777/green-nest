import {apiClient} from "@/lib/api/api-client";import {API_ENDPOINTS} from "@/lib/api/endpoints";import type{Address,AddressRequest}from "@/types/address";
export const addressesApi={get:async()=>(await apiClient.get<Address[]>(API_ENDPOINTS.addresses.root)).data,create:async(p:AddressRequest)=>(await apiClient.post<Address>(API_ENDPOINTS.addresses.root,p)).data,update:async({id,payload}:{id:string;payload:Partial<AddressRequest>})=>(await apiClient.patch<Address>(API_ENDPOINTS.addresses.item(id),payload)).data,remove:async(id:string)=>(await apiClient.delete(API_ENDPOINTS.addresses.item(id))).data};
